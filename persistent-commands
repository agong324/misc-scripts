# this is made for setting up persistent commands and aliases on Unraid, where it should append to /etc/profile 

# change directory and if successful, immediately run ls -la 
cdls() { cd "$@" && ls -la; }

alias ll='ls -la --color=auto'
